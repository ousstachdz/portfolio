import { MouseposiDirective } from './mouseposi.directive';

describe('MouseposiDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseposiDirective();
    expect(directive).toBeTruthy();
  });
});
